# Hardening index

The canonical editable hardening procedure is:

```text
skills/jaxfne-harden/SKILL.md
```

The numbered files in this directory are compatibility pointers retained for
older links. They are not independent doctrine. Update the canonical skill and
the relevant project source document instead of adding rules here.
