# jaxfne skills — friction & contradiction stack

**Purpose:** Durable ledger of known mismatches between skills, docs, code, and
external doctrine. Resolve items here before claiming a skill is authoritative.
**Do not delete rows** — mark `status: resolved` with SHA/date when fixed.

Last audited: 2026-07-02 (F-024 added — default Jupyter kernel points at a different jaxfne checkout; F-007/F-008 re-verified and closed as documented, F-009 re-verified and closed after a one-line `NotImplementedError` message fix pointing at `run_trials`).

---

## Open

| ID | Severity | Topic | Contradiction / friction | Authoritative source (today) | Suggested resolution |
|----|----------|-------|--------------------------|----------------------------|----------------------|
| F-020 | medium | `IzhikevichParams`/pytree treedef is NOT N-independent | `labels`/`layer_labels` are pytree aux data (length N, not leaves) — a treedef built from a differently-sized dummy `construct()` silently produces a corrupted model on `jax.tree_util.tree_unflatten` (no error, just wrong `simulate()` output, confirmed 2026-07-01 by a real bit-identical-output check that failed). Also: `construct()` mutates `Configuration.metadata` in place (`recurrent_backend` key, `circuit.connections[*].status`) — reusing a pre-construct `cfg`'s metadata after a checkpoint reload reproduces a different, still-finite V_m trace, not an error. | `jaxfne/emitters.py` (`IzhikevichParams`/`EdgeList` pytree registration), `scripts/cortical_column_localized_workflow.py::save_column`/`load_column` | Resolved in `save_column`/`load_column`: persist labels/layer_labels/calibration strings + `cfg.metadata` explicitly and reconstruct dataclasses directly, never via a mismatched-N treedef. Any future construct-once/save/reload code must do the same — do not reintroduce the dummy-treedef shortcut. |
| F-023 | low (was medium) | RESOLVED design gap; ONE script's `build_model` duplication remains un-migrated | UPDATE 2026-07-02: the design fix landed. `HDPColumnConfig.base_drive_by_cell_type` is now `Optional[Mapping[str,float]] = None`; `build_model` only passes `baseline_drive_by_cell_type=` to `laminar_cortex_config` when not `None`, so `None` genuinely forwards to the emitter preset (verified via real `build_model()` calls: `None` config → E=5.0/PV=3.0/SST=3.5/VIP=3.0; explicit `BASE_DRIVE_BY_CELL_TYPE_DEFAULT` → 4.0-flat). `apply_drive_correction(model, cfg, mode=)` got a `mode="multiplicative"\|"absolute"` flag (`"multiplicative"` default, byte-identical to the pre-change body; `"absolute"` computes `drive=cfg.base_drive_by_cell_type[ct]*cfg.drive_correction_by_cell_type[ct]`, ignoring the model's existing drive). All 4 pre-existing `HDPColumnConfig(...)` call sites (`scripts/hdp_100_stability_sweep.py`, `scripts/hdp_1000_laminar_column_boosted.py`, `scripts/hdp_v2_rho_sweep.py`, `scripts/hdp_fi_calibration_curves.py`) relied on the old always-4.0-for-all dataclass default and none passed the field explicitly — fixed all 4 to pass `base_drive_by_cell_type=dict(BASE_DRIVE_BY_CELL_TYPE_DEFAULT)` explicitly; re-verified each still produces 4.0-flat drive via a real `build_model()` call post-fix. `scripts/hdp_1000_neuronal_tensor_column.py`'s local `apply_drive_correction` (absolute override) migrated to delegate to `hdp_network.apply_drive_correction(model, cfg, mode="absolute")` — verified BITWISE IDENTICAL to the old hand-rolled loop via a real `build_tensor()`+`construct()` run (max abs diff = 0.0, all 4 cell types checked). `scripts/hdp_suite2_visualizations.py` unaffected (already correctly consolidated; re-verified its `build_model(base.CFG)` still gives 4.0-flat after the fix). | `jaxfne/hdp_network.py::HDPColumnConfig`/`build_model`/`apply_drive_correction`, `scripts/hdp_100_stability_sweep.py`, `scripts/hdp_1000_laminar_column_boosted.py`, `scripts/hdp_v2_rho_sweep.py`, `scripts/hdp_fi_calibration_curves.py`, `scripts/hdp_1000_neuronal_tensor_column.py` | REMAINING GAP (smaller, scoped down from the original 2-script blocker to 1): `scripts/spectrolaminar_tfne_izhikevich_pipeline.py::build_model` still has its own un-migrated `laminar_cortex_config(...)` call (no `baseline_drive_by_cell_type=` passed, correctly relying on the `None` preset fallthrough — this part is not buggy) and was deliberately NOT migrated onto `hdp_network.build_model`/`HDPColumnConfig`, because its network shape is structurally incompatible with `HDPColumnConfig`: 5 layers with a merged `"L2/3"` label (vs `HDPColumnConfig`'s canonical 6-layer `L1..L6` split) and a single flat `cell_type_fractions` dict applied uniformly via `area_layer_cell_types` (vs `HDPColumnConfig`'s per-layer `LAYER_CELL_TYPE_FRAC_DEFAULT` table). This script also never calls `apply_drive_correction` at all (grepped, confirmed), so the mode-flag half of this fix is moot for it. A safe migration needs `HDPColumnConfig` extended to support flat/uniform cell-type fractions and arbitrary layer sets (a second, separate design change) — not attempted this pass to avoid a second silent-science-change risk. `plans.json` item `merge-build_model-apply_drive_correction` updated to `status: partially_done` with the same detail. |
| F-024 | HIGH | The default Jupyter `python3` kernel (and several other registered kernelspecs) point `PYTHONPATH` at a DIFFERENT jaxfne checkout, not this repo | This machine has 9 registered Jupyter kernelspecs (`jupyter kernelspec list`); the plain `python3` one (what `nbclient.NotebookClient(nb, ...)` falls back to when no `kernel_name` is given) has `"env": {"PYTHONPATH": "/Users/hamednejat/workspace/computational/jaxfne"}` hardcoded — a completely separate, independently-evolving clone, NOT `~/workspace/analysis/jaxfne`. Confirmed 2026-07-02: manually executing a notebook this way (no explicit `kernel_name`) silently ran against the WRONG codebase and produced a dramatic, reproducible divergence (HDP `H` runaway to 7.96, firing rate pinned at 2000 Hz) for `jaxfne_etude_no_8_continuous_adaptation.ipynb`'s HDP kernel call — parameters that were rock-solid via a correct `python3 -c` invocation. Only the ONE notebook that directly exercises `simulate_edge_recurrent_izhikevich_hdp` (étude 8) was actually affected this session; études 3/9/10/11/12 (the standard `.construct()`/`jtfne.simulate()` paradigm path) were re-verified bit-identical against the correct checkout, since that code path happens to be unchanged between the two clones — but this was NOT guaranteed and should not be assumed for future notebooks. **The real pytest gate (`tests/test_notebook_execution_suite.py`) was never affected** — `_notebook_exec_helpers.py::portable_kernel_context` builds a throwaway kernelspec from `sys.executable` directly, bypassing all pre-registered kernelspecs entirely; this is why every `test_release_notebook_executes_cleanly` PASS this session was trustworthy even while ad-hoc manual executions were not. | `~/Library/Jupyter/kernels/python3/kernel.json`, any ad-hoc `NotebookClient(nb, ...)` call without an explicit `kernel_name` | Fixed going forward: always pass `kernel_name="jaxfne_analysis"` (the pre-existing, correctly-configured kernel pointed at this repo) to any manual/ad-hoc `NotebookClient` call, or replicate `portable_kernel_context`'s `sys.executable`-based approach. Also print `jaxfne.__file__` (not just `__version__`) as the very first executed line of any manually-run notebook to catch this class of bug immediately rather than trusting plausible-looking numbers. Not fixed: the stale/misconfigured `python3` kernelspec itself remains on disk (out of scope for this repo — it's a local machine config artifact, not repo-tracked). |

---

## Resolved (this pass, 2026-07-02)

| ID | Resolution | Date |
|----|------------|------|
| F-016 | `jaxfne/export.py::save_figure` now emits `warnings.warn("jaxfne.export.save_figure is deprecated; use jaxfne.vis.export_figure instead (handles matplotlib + plotly, does not close the figure).", DeprecationWarning, stacklevel=2)` as its first statement (`import warnings` added to the file's top-level imports). Confirmed `jaxfne/vis/exporters.py::export_figure` is a near-drop-in replacement (matplotlib+plotly dispatch, `formats=`/`dpi=` kwargs) but NOT a pure signature match: `save_figure` takes a `path` with a suffix and an unused-by-export_figure `bbox_inches` kwarg and always closes `fig` after saving, while `export_figure` takes an extensionless `path` + explicit `formats=` tuple, never closes `fig`, and rejects `html` for matplotlib figures — callers migrating manually need to adjust call sites, not just swap the function name. Verified with a real functional smoke test: `jtfne.save_figure(fig, path)` under `warnings.catch_warnings(record=True)` raised the `DeprecationWarning` and still wrote the PNG file to disk. Grepped the whole repo (`grep -rn "save_figure" --include=*.py --include=*.ipynb`) for callers: only tutorial/étude notebooks (13 files, all pre-existing `jtfne.save_figure(...)` calls, left untouched per notebook-JSON-risk policy) and `tests/test_root_export_api_v0338.py` (only checks the symbol exists/is callable, does not invoke it, so the new warning does not affect it) — no `.py` script call sites required migration. `save_figures` (plural, `jaxfne/export.py` line ~42) calls `save_figure` internally per-figure, so it now also emits the warning once per figure — left as-is since `save_figures` is documented DEPRECATED too and this is the correct signal. `pyproject.toml`'s `[tool.pytest.ini_options]` has no `filterwarnings`/`-W error` entry, so the new warning cannot turn any test into a failure. | 2026-07-02 |
| F-007 | Re-verified, not just trusted from the row's own text. `Configuration` is a real bare dataclass (`grep -n "class Configuration" jaxfne/core.py` → line 1109), and `skills/jaxfne-config/SKILL.md` genuinely documents builder-first usage throughout — both Quickstart sections (lines 119–144) use `jtfne.laminar_cortex_config(...)`/`jtfne.build_laminar_column(...)` fluent builders exclusively, never a bare `Configuration(...)` call, and line 27's "Fluent pattern" section states each call returns a new `Configuration` via immutable-style chaining. Genuinely informational, not a bug — closed as documented, no code change needed. | 2026-07-02 |
| F-008 | Re-verified. `grep -n "band_power\|phase_locking" jaxfne/__init__.py jaxfne/core.py` → zero matches; `python3 -c "import jaxfne as jtfne; print('band_power' in dir(jtfne), 'phase_locking' in dir(jtfne))"` → `False False` (a distinct `bandpower_jax` helper exists in the namespace but is not the claimed `band_power`/`phase_locking` objective API). `Objective` (core.py:2854) and `Model.tune` (core.py:5408) both exist. `skills/jaxfne-neural-network/SKILL.md` line 102 states verbatim: "**Not public:** `jtfne.band_power`, `jtfne.phase_locking`, objective `+`/`*` algebra" — exact match to the row's claim — and documents the real `Objective`/`Model.evaluate`/`Model.tune` chain (lines 94-121) including the differentiable-tune-path caveat. Genuinely informational, not a bug — closed as documented, no code change needed. | 2026-07-02 |
| F-009 | Re-verified — this one had a real (small) gap beyond documentation. `jaxfne/core.py::Signals.get` (lines 2773-2778) does raise `NotImplementedError` for `trial=` as claimed. `run_trials` does exist and works as claimed: `Model.run_trials` (core.py:4879) and a top-level `jtfne.run_trials` free function (core.py:8026, delegates to `model.run_trials`) both confirmed via grep, and `python3 -c "import jaxfne as jtfne; print(hasattr(jtfne,'run_trials'))"` → `True`; `jaxfne/tutorial_utils.py::simulate_laminar_trials` also confirmed to exist. `skills/jaxfne-neural-network/SKILL.md` line 70 documents the workaround. But the raised exception's own message text did NOT mention `run_trials`/`tutorial_utils` at all — a user hitting the error directly in code (not reading the skill first) had no pointer to the fix. Applied the safe one-line message fix: `jaxfne/core.py`'s `NotImplementedError` in `Signals.get` now reads "...Use jtfne.run_trials(...)/Model.run_trials(...) or tutorial_utils.simulate_laminar_trials(...) for multi-trial data, or index the array directly once trial semantics are explicit." Verified: `python3 -m py_compile jaxfne/core.py` → exit 0 (no syntax error), and a live runtime check (`Signals.get('V_m', trial=0)` on a synthetic `Signals` instance) raised `NotImplementedError` with the new message text confirmed by direct capture. Grepped `tests/` for any test pinned to the old message text (`grep -rn "trial=.*NotImplementedError\|not supported: core Signals" tests/`) — no hits, so no test needed updating. Closed. | 2026-07-02 |
| F-018 | Added a paragraph to `AGENTS.md` § "Two (now three) paths to a laminar run" (immediately after the "NeuronalTensor-path detail (0.4.7)" HDP paragraph), new subheading "**HDP v2 sign orientation (`hdp_rule=`, ...)**": documents that `signed_linear`/`signed_quadratic` compute their basis as `H_post - H_pre` (not the naively-expected `H_pre - H_post`), citing the exact reason from the inline comment at `jaxfne/emitters.py:1347` — it preserves the postsynaptic-indexing invariant used elsewhere in the kernel (`W_i` from outgoing edges, `I_syn_i` from incoming edges, E/I sign split via `exc_mask`), so a resource-starved postsynaptic neuron still gets weakened excitatory / strengthened inhibitory incoming weights. Also noted while reading the source: the top-level docstring's own rule-family summary (`jaxfne/emitters.py` lines 1057-1060) still states the naive `H_pre - H_post` form and was NOT corrected in this pass (out of scope for this ledger row, which only asked for an `AGENTS.md` note) — a future pass should reconcile the docstring itself. Verified no markdown structure damage from the edit: header count unchanged (19), bracket/paren counts balanced in the edited region. | 2026-07-02 |
| F-022 | Root-caused and fixed in `jaxfne/paradigm.py::omission_oddball_paradigm`: added `metadata={"drive_amplitude": 0.0}` to the three marker-only `ParadigmEvent`s (`trial_start` in all 3 conditions, `post_stimulus` in `expected`/`unexpected`, `post_omission` in `omitted`) — the real stimulus/oddball events (`standard`, `deviant_label`, and the `is_omission=True` omission slot) were left untouched, since `jaxfne/core.py::stimulus_schedule`'s `is_drive = not e.is_omission and e.onset_ms is not None` heuristic already correctly zeroes `is_omission=True` events regardless of metadata, and the labeled stimulus events are supposed to carry the default drive. Used `coop_omission_oddball_paradigm`'s `metadata={"drive_amplitude": 0.0}` idiom as the template (confirmed live during this pass that its own `trial_start` event at `jaxfne/paradigm.py` line ~743 does NOT set that override — only its per-pulse `is_omitted` branch does — so the "already correct on markers" premise from the task brief was not literally true for that function's `trial_start`; the *pattern itself*, applied to `omission_oddball_paradigm`, is still the right fix and is what the row's own suggested-resolution text specified). Caller sweep (`grep -rn "omission_oddball_paradigm(" --include=*.py --include=*.ipynb .` minus `coop_` matches): `tests/test_v0313_omission_oddball_tutorial.py` (3 call sites, assert condition/window structure and `stimulus`/`is_omission` flags only, no `drive_amplitude` assertions), `tutorials/jaxfne_v0313_omission_oddball.ipynb` (calls the paradigm but never touches `stimulus_schedule`/`drive_amplitude` directly), `tutorials/etudes/jaxfne_etude_no_11_omission_local.ipynb` (the paired `expected` vs `omitted` local-omission contrast the row flagged as cancelling-out). Verification: `python3 -m py_compile jaxfne/core.py jaxfne/paradigm.py` → exit 0. `python3 -m pytest tests/test_v0313_omission_oddball_tutorial.py tests/test_stimulus_injection_v012.py tests/test_v0314_ablation_controls.py tests/test_v0341_kernels.py tests/test_coop_v035.py -v` → 28 passed. `python3 -m pytest "tests/test_notebook_execution_suite.py::test_release_notebook_executes_cleanly[etudes/jaxfne_etude_no_11_omission_local.ipynb]"` → 1 passed (both before and after the fix — confirmed the paired contrast's execution is unaffected). `python3 -m pytest tests/test_notebook_execution_suite.py -k "0313"` → 1 passed. Live check post-fix: `expected`/`unexpected`/`omitted` conditions' `trial_start`/`post_stimulus`/`post_omission` events all now carry `metadata["drive_amplitude"] == 0.0`, while `standard`/`deviant`/`omission` slot events are untouched (still default/omission-zeroed). Also softened étude 11's markdown cell (cell index 5) from "CAVEAT (confirmed by direct inspection...)" to "RESOLVED (F-022, 2026-07-02): ...", re-verified notebook is valid JSON and the release-gate test still passes after that edit. No other callers found whose behavior would change beyond the paired-contrast case the row already scoped as safe. | 2026-07-02 |

## Resolved (this pass, 2026-07-01 continued)

| ID | Resolution | Date |
|----|------------|------|
| F-021 | `jaxfne/_pipeline.py::checkpoint_state`/`restore_state` did NOT actually round-trip bfloat16 correctly, contradicting an earlier same-day assumption ("already dtype-transparent, just needs re-verifying with a real bf16 array") in plans.json item `bf16-quantized-tfne-izhikevich-mode` (point 8). Confirmed the failure directly: plain `np.savez`/`np.load` silently mangles ml_dtypes bfloat16 arrays into raw void bytes (`dtype('|V2')`) on read-back — no exception, `restore_state` then crashes downstream when JAX tries to interpret the void array. Fixed: `checkpoint_state` now upcasts bf16 leaves to float32 before saving (exact, no precision loss — bf16 occupies the top 16 bits of float32) and records each leaf's original dtype name in the JSON sidecar (`leaf_dtypes`); `restore_state` casts back down using that name. Verified with a real bf16 `IzhikevichParams` leaf round-trip (dtype match + value match) and a new regression test `tests/test_pipeline_pure_functions.py::test_checkpoint_restore_roundtrip_preserves_bfloat16` (15/15 pass in that file, no regression). | 2026-07-01 |

## Resolved (this pass)

| ID | Resolution | Date |
|----|------------|------|
| F-017 | RESOLVED 2026-07-01: root-caused and fixed. rho_passive/H^2 fails at all 75 swept candidates because it is >=0 everywhere (H_min<H<H_max) -- with DEFAULT_HDP's own gamma=delta=0, no term in dH/dt could ever go negative above H*=1, so H was only ever stopped by the H_max hard clip, not a real restoring force (confirmed: H_max_obs pinned near 10.0 across nearly the whole sweep). Fix: revived `K_ctrl*(1-H_i)` in jaxfne/emitters.py (was dead code -- computed, never used) as a genuine two-sided restoring term. Re-verified at the full 20s/5-seed gate with K_ctrl=1.0 and 5.0: 10/10 rows pass (H_std 0.0002-0.0030, well under the 0.05 bar), and got TIGHTER at 20s than a 5s smoke check (genuine convergence, not slow drift -- the exact failure mode that fooled rho_passive). DEFAULT_HDP's existing K_ctrl=5.0 value is now actually active and validated. F-019 (formula redesign) is resolved by this fix; not opened as a separate row. | 2026-06-29, resolved 2026-07-01 |
| F-001 | HDP E size: code + docs aligned to `DEFAULT_HDP_SIZE_SCALE_BY_CELL_TYPE` | 2026-06-27 |
| F-002 | Layer naming table in `AGENTS.md` + cortical-column skill | 2026-06-27 |
| F-005 | Global `AGENTS.md` JAX §11 gate strings updated | 2026-06-27 |
| F-006 | `jaxfne/skills/` removed | 2026-06-27 |
| F-010 | Worker router skill list aligned | 2026-06-27 |
| F-011 | `layer_celltype_count_table` / `column_density_table` implemented in `builders.py` | 2026-06-27 |
| F-012 | Homeostasis disambiguation in modeling skill | 2026-06-27 |
| F-013 | Plasticity overload in catalog | 2026-06-27 |
| F-014 | v038 projection pedagogy: row-normalize label added to notebook; default confirmed `density_preserving` | 2026-06-27 |
| F-015 | Objective evaluation path in skills | 2026-06-27 |
| — | Extensionless Python "skills" replaced with verified `SKILL.md` files | 2026-06-27 |
| — | `jaxfne-vis-modules` (renamed 2026-06-30 from `jaxfne-visualization-schema`) module map matches `jaxfne/vis/*.py` | 2026-06-27 |
| — | F-001 doc/script sweep (hdp.md, neuronal_tensor.md, hdp script, tutorial 07 equations) | 2026-06-27 |
| — | F-004 doc sweep (étude7, showcases, tutorial 07, changelog) | 2026-06-27 |
| — | F-003 doc sweep (fields.md, probes.md, tensor_field_workflows, v038 tutorial) | 2026-06-27 |
| — | Global skill sync script `skills/SYNC_GLOBAL.sh` + doctrine F-005 | 2026-06-27 |

---

## How to use

1. Before editing a skill, grep this file for the topic.
2. If you fix a friction in code or docs, update the row to `resolved` — do not erase history.
3. New frictions: append with next `F-0XX` ID.
