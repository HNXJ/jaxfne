# jaxfne skills — friction & contradiction stack

**Purpose:** Durable ledger of known mismatches between skills, docs, code, and
external doctrine. Resolve items here before claiming a skill is authoritative.
**Do not delete rows** — mark `status: resolved` with SHA/date when fixed.

Last audited: 2026-07-02 (F-024 added — default Jupyter kernel points at a different jaxfne checkout; F-007/F-008 re-verified and closed as documented, F-009 re-verified and closed after a one-line `NotImplementedError` message fix pointing at `run_trials`).

**F-025 (new, 2026-07-05):** the core.py monolith split (5 slices, 8370 -> 233
lines, landed 2026-07-04/05) makes any OLDER row in this ledger that cites a
specific `jaxfne/core.py:<line>` line number unreliable — core.py is now a
pure re-export aggregator holding no real logic, so those line numbers point
at stale content (e.g. F-023's `Configuration.area_layer_cell_types(...)
(jaxfne/core.py:1866)` citation). The *symbol* still resolves via
`jaxfne.core.X` (full re-export preserved), just not at the cited line — the
real implementation moved to `_config.py`/`_model.py`/`_construct.py`/
`_runtime_config.py`/`_signals.py` (see `jaxfne-worker-context-router`'s
ownership map for which slice owns what). Re-verify any line-number citation
against the actual current file before trusting it; do not assume core.py
line numbers from any row dated before 2026-07-04 are still accurate.

---

## Open

| ID | Severity | Topic | Contradiction / friction | Authoritative source (today) | Suggested resolution |
|----|----------|-------|--------------------------|----------------------------|----------------------|
| F-020 | medium | `IzhikevichParams`/pytree treedef is NOT N-independent | `labels`/`layer_labels` are pytree aux data (length N, not leaves) — a treedef built from a differently-sized dummy `construct()` silently produces a corrupted model on `jax.tree_util.tree_unflatten` (no error, just wrong `simulate()` output, confirmed 2026-07-01 by a real bit-identical-output check that failed). Also: `construct()` mutates `Configuration.metadata` in place (`recurrent_backend` key, `circuit.connections[*].status`) — reusing a pre-construct `cfg`'s metadata after a checkpoint reload reproduces a different, still-finite V_m trace, not an error. | `jaxfne/emitters.py` (`IzhikevichParams`/`EdgeList` pytree registration), `scripts/cortical_column_localized_workflow.py::save_column`/`load_column` | Resolved in `save_column`/`load_column`: persist labels/layer_labels/calibration strings + `cfg.metadata` explicitly and reconstruct dataclasses directly, never via a mismatched-N treedef. Any future construct-once/save/reload code must do the same — do not reintroduce the dummy-treedef shortcut. |
| F-024 | HIGH | The default Jupyter `python3` kernel (and several other registered kernelspecs) point `PYTHONPATH` at a DIFFERENT jaxfne checkout, not this repo | This machine has 9 registered Jupyter kernelspecs (`jupyter kernelspec list`); the plain `python3` one (what `nbclient.NotebookClient(nb, ...)` falls back to when no `kernel_name` is given) has `"env": {"PYTHONPATH": "/Users/hamednejat/workspace/computational/jaxfne"}` hardcoded — a completely separate, independently-evolving clone, NOT `~/workspace/analysis/jaxfne`. Confirmed 2026-07-02: manually executing a notebook this way (no explicit `kernel_name`) silently ran against the WRONG codebase and produced a dramatic, reproducible divergence (HDP `H` runaway to 7.96, firing rate pinned at 2000 Hz) for `jaxfne_etude_no_8_continuous_adaptation.ipynb`'s HDP kernel call — parameters that were rock-solid via a correct `python3 -c` invocation. Only the ONE notebook that directly exercises `simulate_edge_recurrent_izhikevich_hdp` (étude 8) was actually affected this session; études 3/9/10/11/12 (the standard `.construct()`/`jtfne.simulate()` paradigm path) were re-verified bit-identical against the correct checkout, since that code path happens to be unchanged between the two clones — but this was NOT guaranteed and should not be assumed for future notebooks. **The real pytest gate (`tests/test_notebook_execution_suite.py`) was never affected** — `_notebook_exec_helpers.py::portable_kernel_context` builds a throwaway kernelspec from `sys.executable` directly, bypassing all pre-registered kernelspecs entirely; this is why every `test_release_notebook_executes_cleanly` PASS this session was trustworthy even while ad-hoc manual executions were not. | `~/Library/Jupyter/kernels/python3/kernel.json`, any ad-hoc `NotebookClient(nb, ...)` call without an explicit `kernel_name` | Fixed going forward: always pass `kernel_name="jaxfne_analysis"` (the pre-existing, correctly-configured kernel pointed at this repo) to any manual/ad-hoc `NotebookClient` call, or replicate `portable_kernel_context`'s `sys.executable`-based approach. Also print `jaxfne.__file__` (not just `__version__`) as the very first executed line of any manually-run notebook to catch this class of bug immediately rather than trusting plausible-looking numbers. Not fixed: the stale/misconfigured `python3` kernelspec itself remains on disk (out of scope for this repo — it's a local machine config artifact, not repo-tracked). |

---

## Resolved (this pass, 2026-07-14b)

| ID | Resolution | Date |
|----|------------|------|
| F-030 | CLOSED 2026-07-14. Ran `ruff check jaxfne/ --fix` (509 lint errors found) without first identifying every re-export-aggregator file in the package -- `ruff --fix` gutted `jaxfne/core.py` (-209 lines, its entire re-export import block) and `jaxfne/_construct.py` (-78 lines) because F401 "unused import" is a real false positive for files whose entire purpose is re-exporting names for a higher import path (`jaxfne.X`) without referencing them internally -- `import jaxfne` broke outright (`ImportError: cannot import name 'AxisSpec'`), caught immediately by `python3 -c "import jaxfne"` before it reached a commit. Reverted (`git checkout -- jaxfne/ pyproject.toml`), then fixed properly: identified every file with an F401 count high enough to suspect re-export (`ruff check --select F401 --output-format concise | awk -F: '{print $1}' | sort | uniq -c`) plus files with an explicit "canonical source / do not duplicate" comment (`grep -rl "imported (not redefined)\|canonical source"`), added `[tool.ruff.lint.per-file-ignores]` F401 exemptions for `jaxfne/__init__.py`, `core.py`, `_construct.py`, `_signals.py`, `_model.py`, `fields/proxy.py`, `optim/__init__.py`, `vis/__init__.py` in `pyproject.toml` BEFORE re-running `--fix`. Caught a SECOND near-miss on the corrected run: `fields/proxy.py` had only 4 F401 hits (below the "high count" heuristic I'd used for the first pass) but was also a genuine re-export file -- `ruff --fix` removed `_dtype_name`/`_test_kernel_row_normalization` from its `.diagnostics` re-export block, breaking `tests/test_fields_helper_dedup_v0330.py::test_proxy_names_are_identical_objects` (an identity-check test that exists specifically to catch this class of bug) -- caught by the full suite, not by inspection; reverted that one file, added it to the ignore list, re-ran clean. **Lesson, generalized beyond this repo:** low F401 count is NOT a safe proxy for "not a re-export" -- grep for the explicit re-export comment pattern AND cross-check any identity/dedup tests before trusting any lint auto-fix on a package's public-surface files, regardless of how few lines it touches. 509 -> 116 errors (77% reduction), full suite re-verified clean (2661 passed/0 failed) after both corrections, `import jaxfne` + `mkdocs build --strict` + wheel build all clean. | 2026-07-14 |
| F-029 | CLOSED (partial, low-risk subset) 2026-07-14. External 9-issue technical review of `emitters.py::simulate_edge_recurrent_izhikevich_hdp` (the HDP kernel, severity-ranked 🔴🔴🟠🟠🟠🟠🟡🟡🔵). Independently re-verified every issue against the live kernel body, `hdp_network.py`'s `DEFAULT_HDP`/`DEFAULT_HDP_DESYNC`/`DEFAULT_HDP_V1_PFC_AAAB` presets, and `neuronal_tensor.py`, rather than trusting the review's code snippets. **Real, verified, fixed this pass (zero/near-zero behavior change):** (Issue 3) `neuronal_tensor.py`'s module docstring claimed E's relative size default is `2.0`; the actual `DEFAULT_HDP_SIZE_SCALE_BY_CELL_TYPE` it imports as "single source of truth" has `E: 5.0` — stale docstring corrected to `5.0`, the live table (used by every tuned/verified preset) left untouched. (Issue 5) `dH_passive = rho_passive_arr / (H*H)` had no floor on the denominator — inert at every shipped preset (`rho_passive=0.0` everywhere) but produces `0.0/0.0 = NaN` if a caller ever sets `H_min=0.0` (non-default, not currently blocked); guarded with `jnp.maximum(H*H, 1e-8)`, a no-op at the real default `H_min=0.1`. (Issue 9) `_hdp_size_scale_array` ran an O(n_neurons) Python loop on every `simulate()` call (setup-only, not per-scan-step, so the reviewer's "inside a JAX setup path" framing was accurate but the severity was correctly rated lowest/🔵) — memoized via `functools.lru_cache` keyed on `(labels, overrides, dtype)` so repeated `simulate()` calls on the same tensor (the common case in tuning/sweep loops) skip the loop entirely; first fix attempt used `str(dtype)` as the cache-key/numpy-dtype string and broke on `jax.numpy` dtype objects (`TypeError: data type "<class 'jax.numpy.float32'>" not understood`), caught immediately by the targeted test run and corrected to `jnp.dtype(dtype).name`. **Real observations, documented in-place (no behavior change, correctly NOT applied as code fixes this pass):** (Issue 1) `C_spike`'s discrete H-drain is intentionally not divided by `tau_i` per the function's own pre-existing docstring — real inconsistency with the continuous path's size-speed contract *if* `C_spike` is ever set nonzero, but `C_spike=0.0` in every shipped preset today (confirmed via `BASE_HDP_KWARGS_DEFAULT`/`DEFAULT_HDP_DESYNC`), so currently inert everywhere; flagged with an inline comment, not changed, since scaling it would require re-verifying every preset that might someday enable it. (Issue 2) `H_boost_gain>0` combined with `gamma>0` can create a starved-neuron-fires-more-but-drains-more loop since the boosted extra current isn't counted in `dH_income` — real, and `DEFAULT_HDP_V1_PFC_AAAB` genuinely combines both (`H_boost_gain=4.0` from `BASE_HDP_KWARGS_DEFAULT`, `gamma=0.5` from `DEFAULT_HDP_DESYNC`) — but that preset's own comments document it as empirically stabilized via `K_w_ctrl` bounding weight growth (20-chained-trial verification already on record), not via this income-term symmetry, so no code change without re-running that verification; documented inline instead. (Issue 4) `W_burden` pools E+I outgoing weight into one abs-sum, making it insensitive to E/I redistribution — real, but this is a defensible "both directions cost metabolic resources" design choice, not an obvious bug; splitting it into signed E/I terms would change every tuned preset's dynamics — documented inline, not changed. (Issue 6) the multiplicative weight-update rule (`dw ~ rule_basis * wmag`) makes edges near `w_floor` nearly frozen (vanishing-gradient-like) — real and matches the reviewer's description exactly, but switching to additive would change every tuned preset's dynamics — documented inline, not changed. (Issue 7) `H_boost_gain`'s multiplier uses the carry (previous-step) H rather than the same step's `H_next`, a one-step lag — real, reorderable in principle (`H_next` doesn't depend on this step's own spikes), but changing it alters numeric output for every `H_boost_gain>0` run (including `DEFAULT_HDP_V1_PFC_AAAB`) — documented inline as intentional/negligible-at-shipped-dt, not changed. (Issue 8) the review's claim that the *function's own* kwarg defaults are `barrier_c=barrier_d=0.01` was traced to a mis-citation — the function signature's real defaults are both `0.0` (confirmed: inert unless set), but `hdp_network.py`'s `DEFAULT_HDP` **preset** genuinely does set both to `0.01`, a 1:1 ratio, not the 100:1 ratio the function's own docstring says is required for the barrier's minimum to coincide with `H*=1` — a real, previously-undocumented gap between the docstring's stated requirement and the shipped preset; documented in both `emitters.py`'s docstring and a new comment directly on `DEFAULT_HDP` in `hdp_network.py`, left unchanged because `DEFAULT_HDP`'s verified dynamics keep H tightly pinned near `H*=1` via `K_ctrl` (H rarely nears either boundary in practice, so the barrier asymmetry is dormant, not exercised) — re-tune before relying on barrier repulsion near a boundary. Verified: `python3 -c "import ast; ..."` clean parse on all 3 touched files (`emitters.py`, `hdp_network.py`, `neuronal_tensor.py`); targeted run (`pytest -k "hdp or emitter or izhikevich or neuronal_tensor"`) caught the `str(dtype)` cache-key bug immediately, 182/182 passed after the fix; full fast suite re-run clean: `2661 passed, 74 skipped, 34 deselected, 4 xfailed, 0 FAILED/ERROR` (`grep -c "^FAILED\|^ERROR"` on raw, untruncated output → `0`). | 2026-07-14 |
| F-028 | CLOSED (partial, low-risk subset) 2026-07-13. External deep-code review of `emitters.py`/`_model.py`/`_construct.py`/`_config.py`/`_signals.py`/`connectivity.py` (16 findings, severity-ranked). Independently re-verified every finding against live source rather than trusting the review -- several were conflated between two different `Model` methods (`simulate()` vs `simulate_batch()`) or overstated relative to actual JAX tracing semantics. **Real, verified, fixed this pass (low-risk):** (1) `jaxfne/emitters.py` nested identical `try/except ValueError` around `register_pytree_node` -- dead outer block, collapsed to one. (2) `jaxfne/connectivity.py`'s per-edge tau lookup was a Python-level list comprehension (`[tau_by_mech[int(i)] for i in mech_idx]`) -- replaced with a vectorized `jnp.asarray(tau_by_mech)[mech_idx]` gather, same output, no Python loop. (3) `jaxfne/emitters.py::_get_cell_type_params` silently fell back to VIP parameters for an unrecognized cell-type name -- now raises `ValueError` naming the allowed types. **Correction to my own first pass:** the initial targeted test batch (138 emitter/connectivity/izhikevich tests) passed clean, but the FULL suite then surfaced 16 real errors in `tests/test_v033_api_extensions.py` -- `"I"` (generic Inhibitory) turned out to be a real, widely-used two-population shorthand across `examples/`, `scripts/evidence_figures/` (production evidence-generation scripts), and multiple test files, previously silently resolving to VIP params via the exact fallback just removed. Re-verified via a repo-wide grep of every `cell_type(s)={...}` dict literal (`jaxfne/`, `scripts/`, `examples/`, `tests/`, `tutorials/`) -- only 5 keys ever used anywhere: `E`, `I`, `PV`, `SST`, `VIP`. Added `"I"` as an explicit alias to VIP's exact parameter values (zero behavior change for any real caller, just no longer accidental) rather than leaving it to raise. Lesson: "ran a plausible-looking subset, got green" is not the same claim as "verified", full-suite re-run is what actually caught this. **Real, verified, deliberately NOT fixed this pass (too large/risky to rush):** the Izhikevich step dynamics (`dv = 0.04*v*v + 5.0*v + 140.0 - u + current_native`) is duplicated **11 times** across `emitters.py`'s scan-body closures (worse than the review's claimed 5) -- extracting a single `_izhikevich_step` helper is the single highest-value refactor in the file, but touches the hottest JIT-traced path in the package across 11 call sites with differing carry shapes; needs its own scoped session with full before/after behavioral-equivalence verification per call site, not a rushed extraction bundled into a review-response pass. Also open: `Model.simulate_batch()` (distinct from `Model.simulate()`, which already handles this correctly via `_simulate_arrays()`'s cache keys) never reads `sim.ablation` at all, in any of its internal paths -- silently ignores a documented `Simulation` field rather than applying or rejecting it. **Confirmed false/overstated, no action:** the review's "circular import hazard" (finding 1) is the standard deferred-import pattern, not fragile, given no top-level `_model.py -> core/_construct` import exists; `_segment_sum` "Python dispatch overhead" (finding 5) is a non-issue since `lax.scan` traces once; `object.__setattr__` on frozen `Model` (finding 7) is currently safe since `Model` is never used as a `jax.jit` static/traced argument anywhere in the codebase; `simulate()` "returns untyped dict" (finding 15) is false -- it has a `-> Signals` typed return, the review conflated it with `simulate_batch()`'s genuinely-dict return. Verified: py_compile clean, 138 targeted tests + 37 canonical-biophysics/field-admissibility tests pass, full fast suite re-run clean. | 2026-07-13 |
| F-027 | CLOSED 2026-07-13. Full re-verification of all 13 `skills/*/SKILL.md` files against live source, prompted by an external agent's API-coverage review (13 files scored, 3 independently spot-checked at 94-96, 6 flagged as unverified/thin-relative-to-surface). Re-verified those 6 plus `jaxfne-worker-context-router` directly against source: `jaxfne-objective-grammar`, `jaxfne-spectrolaminar-suite`, `jaxfne-modeling-optimization-schema`, and `jaxfne-neural-network` all fully accurate on every falsifiable claim checked (function existence, `Signals.get(trial=)` exact error text, `project_laminar_sources` default mode, `compile_connection_rules`/`neuron_table()` real signatures, `Model.__call__` absence at the instance level). `jaxfne-sha256-artifact-integrity`'s apparent "6 missing functions" was a false alarm from my own test methodology -- that skill provides copy-paste reference recipes (`hashlib`/`json` patterns), not claims about pre-built `jtfne.*` API, confirmed by re-reading its own stated purpose. **One real, material finding:** `jaxfne-config`'s canonical-column table (`≈77E:23I` overall, "PV concentrates in L4, absent in L1") was stale and off by up to 25 percentage points per layer against the live `jtfne.CANONICAL_LAYER_CELL_TYPE_FRACTIONS` constant and a fresh `construct(cfg).neuron_table()` run (real: **≈66E:34I** overall; PV peaks at L2/L3 at 25% each, is absent at **L6** not L1). Traced to the true origin: `jaxfne/builders.py`'s own docstring (2 locations) had the identical stale numbers -- fixed there first, then in `skills/jaxfne-config/SKILL.md`, then in the 5 more places the stale figure had propagated: `docs/quickstart.md`, `docs/api/index.md`, `docs/guides/configuration_grammar.md`, `docs/tutorials/07_jaxfne_suite_no_2_spectrolaminar_motif.md` (including its worked-example dict), and `docs/tutorials/13_canonical_column_etude.md` (table, worked-example dict, two prose mentions, one plot title). `jaxfne-worker-context-router`'s `core.py` line count (`233` claimed vs `237` live) corrected as a minor drift. No code behavior changed anywhere -- this was a documentation/skill-accuracy pass, not a package fix. Verified: full fast suite 2661 passed/0 failed after the fix; `mkdocs build --strict` clean; `py_compile` clean on `jaxfne/builders.py`. | 2026-07-13 |
| F-026 | CLOSED 2026-07-08, same precedent as F-023: reclassified `scripts/evidence_figures/`'s "18 of 19 files call matplotlib directly, outside jaxfne/vis/" from "grammar violation needs migrating" to "intentional design boundary, correctly not migrated". Sampled 3 files across the fig0X (architecture-diagram) and ed0X (evidence-receipt) families (`fig01_architecture.py`, `fig05_runtime_scaling.py`, `ed03_notebook_execution_receipts.py`, `ed09_failure_modes_and_nulls.py`) via `grep -n "plt\.\|matplotlib"`: every file follows the identical shape — ONE `plt.subplots(...)` call to open a canvas, then bespoke one-off illustration code specific to that single figure (`FancyArrowPatch`/`FancyBboxPatch` architecture boxes, custom receipt-table layouts, failure-mode diagrams), then `save_matplotlib_figure(fig, FIGURE_PATH, dpi=150)` — already routed through `jaxfne/vis/evidence_manifest.py`/`evidence_export.py` since the 2026-07-07 vis-evidence-bridge commit (`82610ea`). `jaxfne/vis/` exists to make *simulation-output* plotting (rasters, LFP/CSD, spectrolaminar) reusable across scripts/notebooks; these are one-off publication/documentation illustrations that will never be called from anywhere else in the codebase — forcing their bespoke `ax.add_patch(...)`/layout code into the public `jaxfne.vis` module would bloat that module's public surface with non-reusable single-use drawing code, which is worse grammar, not better. `jaxfne-modular-grammar` rule 2's grep-based check does not distinguish "simulation-output plot" from "one-off illustration", which is why it flagged this — the rule itself needs no change, just this documented carve-out (same conclusion pattern as F-023's `HDPColumnConfig` boundary). No code changed; `scripts/evidence_figures/README.md`'s existing documented-exception note (added in the step-7 P1 pass) already states this, this row just supplies the receipt trail justifying it as a closed architectural decision rather than an open gap plans.json step 8 should keep charging points against. | 2026-07-08 |
| F-023 | CLOSED 2026-07-03, reclassified from "remaining gap needs a fix" to "intentional design boundary, documented not migrated". `scripts/spectrolaminar_tfne_izhikevich_pipeline.py::build_model` uses `jtfne.laminar_cortex_config(...)`/`.area_layer_cell_types(...)` directly (the lower-level `Configuration` fluent builder), not `hdp_network.build_model()`/`HDPColumnConfig`, because its network shape is genuinely outside what `HDPColumnConfig` is designed to express: 5 layers with a merged `"L2/3"` label vs `HDPColumnConfig.LAYERS_DEFAULT`'s canonical 6-layer `["L1","L2","L3","L4","L5","L6"]` (`jaxfne/hdp_network.py:24`), and a single flat `cell_type_fractions` dict applied uniformly across all layers vs `HDPColumnConfig`'s per-layer fraction table. Verified `Configuration.area_layer_cell_types(area, layer_cell_types)` (`jaxfne/core.py:1866`) genuinely accepts arbitrary layer names and per-layer dicts, confirming the script's direct-builder approach is a correct, intentional use of the flexible lower-level API, not a broken/incomplete migration. Extending `HDPColumnConfig` to also support non-canonical layer sets and flat cell-type fractions was considered and rejected: `HDPColumnConfig` exists specifically to be the simple/canonical/common-case wrapper (see the 0.4.7 "simple code, minimum complexity" design pass, `plans.json` item `config-schema-ic-pcb-redesign`) — forcing it to cover every possible layer/fraction shape would add complexity to the wrong layer. Documented this boundary in `AGENTS.md` and the `jaxfne-config`/`jaxfne-neural-tensor` skills so a future session doesn't re-attempt an unwanted consolidation. `plans.json` item `merge-build_model-apply_drive_correction` closed as `status: done` (the drive-correction half was already fully consolidated; this half is correctly non-consolidated by design). | 2026-07-03 |
| F-016 | `jaxfne/export.py::save_figure` now emits `warnings.warn("jaxfne.export.save_figure is deprecated; use jaxfne.vis.export_figure instead (handles matplotlib + plotly, does not close the figure).", DeprecationWarning, stacklevel=2)` as its first statement (`import warnings` added to the file's top-level imports). Confirmed `jaxfne/vis/exporters.py::export_figure` is a near-drop-in replacement (matplotlib+plotly dispatch, `formats=`/`dpi=` kwargs) but NOT a pure signature match: `save_figure` takes a `path` with a suffix and an unused-by-export_figure `bbox_inches` kwarg and always closes `fig` after saving, while `export_figure` takes an extensionless `path` + explicit `formats=` tuple, never closes `fig`, and rejects `html` for matplotlib figures — callers migrating manually need to adjust call sites, not just swap the function name. Verified with a real functional smoke test: `jtfne.save_figure(fig, path)` under `warnings.catch_warnings(record=True)` raised the `DeprecationWarning` and still wrote the PNG file to disk. Grepped the whole repo (`grep -rn "save_figure" --include=*.py --include=*.ipynb`) for callers: only tutorial/étude notebooks (13 files, all pre-existing `jtfne.save_figure(...)` calls, left untouched per notebook-JSON-risk policy) and `tests/test_root_export_api_v0338.py` (only checks the symbol exists/is callable, does not invoke it, so the new warning does not affect it) — no `.py` script call sites required migration. `save_figures` (plural, `jaxfne/export.py` line ~42) calls `save_figure` internally per-figure, so it now also emits the warning once per figure — left as-is since `save_figures` is documented DEPRECATED too and this is the correct signal. `pyproject.toml`'s `[tool.pytest.ini_options]` has no `filterwarnings`/`-W error` entry, so the new warning cannot turn any test into a failure. | 2026-07-02 |
| F-007 | Re-verified, not just trusted from the row's own text. `Configuration` is a real bare dataclass (`grep -n "class Configuration" jaxfne/core.py` → line 1109), and `skills/jaxfne-config/SKILL.md` genuinely documents builder-first usage throughout — both Quickstart sections (lines 119–144) use `jtfne.laminar_cortex_config(...)`/`jtfne.build_laminar_column(...)` fluent builders exclusively, never a bare `Configuration(...)` call, and line 27's "Fluent pattern" section states each call returns a new `Configuration` via immutable-style chaining. Genuinely informational, not a bug — closed as documented, no code change needed. | 2026-07-02 |
| F-008 | Re-verified. `grep -n "band_power\|phase_locking" jaxfne/__init__.py jaxfne/core.py` → zero matches; `python3 -c "import jaxfne as jtfne; print('band_power' in dir(jtfne), 'phase_locking' in dir(jtfne))"` → `False False` (a distinct `bandpower_jax` helper exists in the namespace but is not the claimed `band_power`/`phase_locking` objective API). `Objective` (core.py:2854) and `Model.tune` (core.py:5408) both exist. `skills/jaxfne-neural-network/SKILL.md` line 102 states verbatim: "**Not public:** `jtfne.band_power`, `jtfne.phase_locking`, objective `+`/`*` algebra" — exact match to the row's claim — and documents the real `Objective`/`Model.evaluate`/`Model.tune` chain (lines 94-121) including the differentiable-tune-path caveat. Genuinely informational, not a bug — closed as documented, no code change needed. | 2026-07-02 |
| F-009 | Re-verified — this one had a real (small) gap beyond documentation. `jaxfne/core.py::Signals.get` (lines 2773-2778) does raise `NotImplementedError` for `trial=` as claimed. `run_trials` does exist and works as claimed: `Model.run_trials` (core.py:4879) and a top-level `jtfne.run_trials` free function (core.py:8026, delegates to `model.run_trials`) both confirmed via grep, and `python3 -c "import jaxfne as jtfne; print(hasattr(jtfne,'run_trials'))"` → `True`; `jaxfne/tutorial_utils.py::simulate_laminar_trials` also confirmed to exist. `skills/jaxfne-neural-network/SKILL.md` line 70 documents the workaround. But the raised exception's own message text did NOT mention `run_trials`/`tutorial_utils` at all — a user hitting the error directly in code (not reading the skill first) had no pointer to the fix. Applied the safe one-line message fix: `jaxfne/core.py`'s `NotImplementedError` in `Signals.get` now reads "...Use jtfne.run_trials(...)/Model.run_trials(...) or tutorial_utils.simulate_laminar_trials(...) for multi-trial data, or index the array directly once trial semantics are explicit." Verified: `python3 -m py_compile jaxfne/core.py` → exit 0 (no syntax error), and a live runtime check (`Signals.get('V_m', trial=0)` on a synthetic `Signals` instance) raised `NotImplementedError` with the new message text confirmed by direct capture. Grepped `tests/` for any test pinned to the old message text (`grep -rn "trial=.*NotImplementedError\|not supported: core Signals" tests/`) — no hits, so no test needed updating. Closed. | 2026-07-02 |
| F-018 | Added a paragraph to `AGENTS.md` § "Two (now three) paths to a laminar run" (immediately after the "NeuronalTensor-path detail (0.4.7)" HDP paragraph), new subheading "**HDP v2 sign orientation (`hdp_rule=`, ...)**": documents that `signed_linear`/`signed_quadratic` compute their basis as `H_post - H_pre` (not the naively-expected `H_pre - H_post`), citing the exact reason from the inline comment at `jaxfne/emitters.py:1347` — it preserves the postsynaptic-indexing invariant used elsewhere in the kernel (`W_i` from outgoing edges, `I_syn_i` from incoming edges, E/I sign split via `exc_mask`), so a resource-starved postsynaptic neuron still gets weakened excitatory / strengthened inhibitory incoming weights. Also noted while reading the source: the top-level docstring's own rule-family summary (`jaxfne/emitters.py` lines 1057-1060) still states the naive `H_pre - H_post` form and was NOT corrected in this pass (out of scope for this ledger row, which only asked for an `AGENTS.md` note) — a future pass should reconcile the docstring itself. Verified no markdown structure damage from the edit: header count unchanged (19), bracket/paren counts balanced in the edited region. | 2026-07-02 |
| F-022 | Root-caused and fixed in `jaxfne/paradigm.py::omission_oddball_paradigm`: added `metadata={"drive_amplitude": 0.0}` to the three marker-only `ParadigmEvent`s (`trial_start` in all 3 conditions, `post_stimulus` in `expected`/`unexpected`, `post_omission` in `omitted`) — the real stimulus/oddball events (`standard`, `deviant_label`, and the `is_omission=True` omission slot) were left untouched, since `jaxfne/core.py::stimulus_schedule`'s `is_drive = not e.is_omission and e.onset_ms is not None` heuristic already correctly zeroes `is_omission=True` events regardless of metadata, and the labeled stimulus events are supposed to carry the default drive. Used `coop_omission_oddball_paradigm`'s `metadata={"drive_amplitude": 0.0}` idiom as the template (confirmed live during this pass that its own `trial_start` event at `jaxfne/paradigm.py` line ~743 does NOT set that override — only its per-pulse `is_omitted` branch does — so the "already correct on markers" premise from the task brief was not literally true for that function's `trial_start`; the *pattern itself*, applied to `omission_oddball_paradigm`, is still the right fix and is what the row's own suggested-resolution text specified). Caller sweep (`grep -rn "omission_oddball_paradigm(" --include=*.py --include=*.ipynb .` minus `coop_` matches): `tests/test_v0313_omission_oddball_tutorial.py` (3 call sites, assert condition/window structure and `stimulus`/`is_omission` flags only, no `drive_amplitude` assertions), `tutorials/jaxfne_v0313_omission_oddball.ipynb` (calls the paradigm but never touches `stimulus_schedule`/`drive_amplitude` directly), `tutorials/etudes/jaxfne_etude_no_11_omission_local.ipynb` (the paired `expected` vs `omitted` local-omission contrast the row flagged as cancelling-out). Verification: `python3 -m py_compile jaxfne/core.py jaxfne/paradigm.py` → exit 0. `python3 -m pytest tests/test_v0313_omission_oddball_tutorial.py tests/test_stimulus_injection_v012.py tests/test_v0314_ablation_controls.py tests/test_v0341_kernels.py tests/test_coop_v035.py -v` → 28 passed. `python3 -m pytest "tests/test_notebook_execution_suite.py::test_release_notebook_executes_cleanly[etudes/jaxfne_etude_no_11_omission_local.ipynb]"` → 1 passed (both before and after the fix — confirmed the paired contrast's execution is unaffected). `python3 -m pytest tests/test_notebook_execution_suite.py -k "0313"` → 1 passed. Live check post-fix: `expected`/`unexpected`/`omitted` conditions' `trial_start`/`post_stimulus`/`post_omission` events all now carry `metadata["drive_amplitude"] == 0.0`, while `standard`/`deviant`/`omission` slot events are untouched (still default/omission-zeroed). Also softened étude 11's markdown cell (cell index 5) from "CAVEAT (confirmed by direct inspection...)" to "RESOLVED (F-022, 2026-07-02): ...", re-verified notebook is valid JSON and the release-gate test still passes after that edit. No other callers found whose behavior would change beyond the paired-contrast case the row already scoped as safe. | 2026-07-02 |

## Resolved (this pass, 2026-07-01 continued)

| ID | Resolution | Date |
|----|------------|------|
| F-021 | `jaxfne/_pipeline.py::checkpoint_state`/`restore_state` did NOT actually round-trip bfloat16 correctly, contradicting an earlier same-day assumption ("already dtype-transparent, just needs re-verifying with a real bf16 array") in plans.json item `bf16-quantized-tfne-izhikevich-mode` (point 8). Confirmed the failure directly: plain `np.savez`/`np.load` silently mangles ml_dtypes bfloat16 arrays into raw void bytes (`dtype('|V2')`) on read-back — no exception, `restore_state` then crashes downstream when JAX tries to interpret the void array. Fixed: `checkpoint_state` now upcasts bf16 leaves to float32 before saving (exact, no precision loss — bf16 occupies the top 16 bits of float32) and records each leaf's original dtype name in the JSON sidecar (`leaf_dtypes`); `restore_state` casts back down using that name. Verified with a real bf16 `IzhikevichParams` leaf round-trip (dtype match + value match) and a new regression test `tests/test_pipeline_pure_functions.py::test_checkpoint_restore_roundtrip_preserves_bfloat16` (15/15 pass in that file, no regression). | 2026-07-01 |

## Resolved (this pass)

| ID | Resolution | Date |
|----|------------|------|
| F-017 | RESOLVED 2026-07-01: root-caused and fixed. rho_passive/H^2 fails at all 75 swept candidates because it is >=0 everywhere (H_min<H<H_max) -- with DEFAULT_HDP's own gamma=delta=0, no term in dH/dt could ever go negative above H*=1, so H was only ever stopped by the H_max hard clip, not a real restoring force (confirmed: H_max_obs pinned near 10.0 across nearly the whole sweep). Fix: revived `K_ctrl*(1-H_i)` in jaxfne/emitters.py (was dead code -- computed, never used) as a genuine two-sided restoring term. Re-verified at the full 20s/5-seed gate with K_ctrl=1.0 and 5.0: 10/10 rows pass (H_std 0.0002-0.0030, well under the 0.05 bar), and got TIGHTER at 20s than a 5s smoke check (genuine convergence, not slow drift -- the exact failure mode that fooled rho_passive). DEFAULT_HDP's existing K_ctrl=5.0 value is now actually active and validated. F-019 (formula redesign) is resolved by this fix; not opened as a separate row. | 2026-06-29, resolved 2026-07-01 |
| F-001 | HDP E size: code + docs aligned to `DEFAULT_HDP_SIZE_SCALE_BY_CELL_TYPE` | 2026-06-27 |
| F-002 | Layer naming table in `AGENTS.md` + cortical-column skill | 2026-06-27 |
| F-005 | Global `AGENTS.md` JAX §11 gate strings updated | 2026-06-27 |
| F-006 | `jaxfne/skills/` removed | 2026-06-27 |
| F-010 | Worker router skill list aligned | 2026-06-27 |
| F-011 | `layer_celltype_count_table` / `column_density_table` implemented in `builders.py` | 2026-06-27 |
| F-012 | Homeostasis disambiguation in modeling skill | 2026-06-27 |
| F-013 | Plasticity overload in catalog | 2026-06-27 |
| F-014 | v038 projection pedagogy: row-normalize label added to notebook; default confirmed `density_preserving` | 2026-06-27 |
| F-015 | Objective evaluation path in skills | 2026-06-27 |
| — | Extensionless Python "skills" replaced with verified `SKILL.md` files | 2026-06-27 |
| — | `jaxfne-vis-modules` (renamed 2026-06-30 from `jaxfne-visualization-schema`) module map matches `jaxfne/vis/*.py` | 2026-06-27 |
| — | F-001 doc/script sweep (hdp.md, neuronal_tensor.md, hdp script, tutorial 07 equations) | 2026-06-27 |
| — | F-004 doc sweep (étude7, showcases, tutorial 07, changelog) | 2026-06-27 |
| — | F-003 doc sweep (fields.md, probes.md, tensor_field_workflows, v038 tutorial) | 2026-06-27 |
| — | Global skill sync script `skills/SYNC_GLOBAL.sh` + doctrine F-005 | 2026-06-27 |

---

## How to use

1. Before editing a skill, grep this file for the topic.
2. If you fix a friction in code or docs, update the row to `resolved` — do not erase history.
3. New frictions: append with next `F-0XX` ID.
