# Compatibility pointer

Repository orientation is consolidated in
`skills/jaxfne-harden/SKILL.md` and `AGENTS.md`.

Use `scripts/repo_state_snapshot.py` for current checkout facts. This file is
retained only so older links do not become dead links.
