# Compatibility pointer

Runtime and fallback procedure is consolidated in
`skills/jaxfne-harden/SKILL.md`, under “Runtime and fallback behavior”.

Report effective runtime state; do not infer it from requested configuration.
