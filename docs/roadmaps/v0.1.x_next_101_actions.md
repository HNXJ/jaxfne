# jaxfne — Next 101 Actions
**Base state:** `dev @ f567eda`, version `0.1.0`, 178 tests passing, truth gates frozen.
**truth_mode:** truth_safe_unverified
**Generated:** 2026-05-18

---

## Phase 0 — Release Pipeline (Actions 1–7)

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 1 | Push `dev` to remote origin | — | `git push origin dev` |
| 2 | Fast-forward `main` → `dev` HEAD | — | `git checkout main && git merge --ff-only dev` |
| 3 | Push `main` to remote | — | `git push origin main` |
| 4 | Create annotated tag `v0.1.0` | — | `git tag -a v0.1.0 -m "jaxfne v0.1.0 practical OOP core freeze"` |
| 5 | Push tag to origin | — | `git push origin v0.1.0` |
| 6 | Refresh editable install metadata | — | `pip install -e .` → verify `pip show jaxfne` → `0.1.0` |
| 7 | Verify full test suite post-tag | tests/ | `pytest -q` → **178 passed** |

---

## Phase 1 — v0.1.1 Synaptic Kernel (Actions 8–28)

> Implements receptor-indexed exponential synaptic dynamics (design finalized in memory).

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 8 | Create branch `dev-v0.1.1-synaptic-kernel` from `main` | — | `git checkout -b dev-v0.1.1-synaptic-kernel main` |
| 9 | Add `RuntimeConfig.synaptic_kernel` field (`"exponential"` default, accepts `"receptor_exponential"`) | `jaxfne/core.py` | compileall |
| 10 | Add `TAU_BY_RECEPTOR` table: AMPA=2ms, GABA_A=6ms, NMDA=80ms, GABA_B=150ms | `jaxfne/emitters.py` | unit assertion |
| 11 | Add `simulate_receptor_exponential_izhikevich()` — `lax.scan`, `syn_state[n_edges]`, per-edge tau lookup | `jaxfne/emitters.py` | shape/finite test |
| 12 | Branch `_simulate_arrays()` on `synaptic_kernel == "receptor_exponential"` | `jaxfne/core.py` | compileall |
| 13 | Propagate `synaptic_kernel` into `Signals.metadata` | `jaxfne/core.py` | JSON-safe assertion |
| 14 | Export `simulate_receptor_exponential_izhikevich` from `__init__.py` | `jaxfne/__init__.py` | import smoke |
| 15 | Write test A: `RuntimeConfig.synaptic_kernel` validation (default / accept / reject) | `tests/test_synaptic_kernel_v011.py` | pytest |
| 16 | Write test B: `TAU_BY_RECEPTOR` lookup maps receptor_index 0–3 correctly | same | pytest |
| 17 | Write test C: single-edge impulse decay matches `exp(-dt/tau)` | same | pytest |
| 18 | Write test D: `segment_sum` vs manual loop no-double-count equivalence | same | pytest |
| 19 | Write test E: deterministic — same seed → identical `(V_m, spikes, syn_state)` | same | pytest |
| 20 | Write test F: all outputs finite (V_m, spikes, sources, syn_state) | same | pytest |
| 21 | Write test G: default `"exponential"` path is unchanged — existing 178 tests still pass | same | pytest |
| 22 | Write test H: manifest JSON-safe with `synaptic_kernel` key present | same | pytest |
| 23 | Write test I: all 8 truth gates unchanged under `receptor_exponential` path | same | pytest |
| 24 | Write test J: `source_projection_mode` and `field_solver_status` unchanged | same | pytest |
| 25 | Bump version `0.1.0` → `0.1.1` in `core.py` and `pyproject.toml` | both | `test_package_version_alignment` |
| 26 | Add CHANGELOG v0.1.1 entry (receptor-exponential kernel, uncalibrated, scaffold-only) | `CHANGELOG.md` | lint |
| 27 | Run full suite → **188 passed** (178 + 10 new) | — | pytest |
| 28 | Commit + push `dev-v0.1.1-synaptic-kernel`; roll dev backup | — | `git push origin dev-v0.1.1-synaptic-kernel` |

---

## Phase 2 — Source / Field Physics Hardening (Actions 29–45)

> Closes gaps identified in `docs/roadmaps/v0.0.18_longterm/02_source_field_physics_plan_steps_001_100.md`.

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 29 | Audit `fields.py` for CSD sign convention (sink negative, source positive) | `jaxfne/fields.py` | code review |
| 30 | Add `test_csd_sign_convention` — positive dipole → negative sink at source depth | `tests/test_source_field_physics.py` | pytest |
| 31 | Add `test_source_conservation` — `sum(sources, axis=-1)` ≈ 0 for each timestep | same | pytest |
| 32 | Add `test_mean_zero_neumann` — field output mean ≈ 0 across probe contacts | same | pytest |
| 33 | Add `test_no_physical_amplitude_claim` — probe output magnitude unbounded, no `μV` or `mV` label in metadata | same | pytest |
| 34 | Add `test_field_gauge_metadata` — `Signals.metadata["gauge"]` == `"mean_zero"` | same | pytest |
| 35 | Add `test_field_boundary_metadata` — `Signals.metadata["boundary"]` == `"mean_zero_neumann"` | same | pytest |
| 36 | Add `test_source_calibration_label_in_manifest` — `source_calibration_status` == `"uncalibrated_izhikevich_native_current"` | same | pytest |
| 37 | Add `test_no_synaptic_double_counting` — sources are sum over pre-synaptic output, not duplicated per post | same | pytest |
| 38 | Audit `fields.py` conductivity tensor — confirm resistive scalar only (no SPD tensor) | `jaxfne/fields.py` | code review |
| 39 | Add `test_conductivity_scalar_not_tensor` — conductivity value is a float scalar in metadata | `tests/test_source_field_physics.py` | pytest |
| 40 | Document field solver status ceiling in `docs/DOCTRINE.md` — `laminar_proxy_no_pde` is permanent until PDE branch | `docs/DOCTRINE.md` | review |
| 41 | Add `test_field_solver_status_ceiling` — `Signals.metadata["field_solver_status"]` == `"laminar_proxy_no_pde"` | `tests/test_source_field_physics.py` | pytest |
| 42 | Add `test_probe_contact_shape` — probe output shape is `(n_steps, n_contacts)` for all field configs | same | pytest |
| 43 | Add `test_field_domain_laminar_column` — `domain == "laminar_column"` in config round-trips to metadata | same | pytest |
| 44 | Commit source/field hardening block | — | 188 + 15 = **203 passed** |
| 45 | Push to `dev`; update AGENTS.md completed-work log | `AGENTS.md` | push |

---

## Phase 3 — Emitter / Network Tests Hardening (Actions 46–60)

> Targets `03_emitters_cells_networks_plan_steps_101_200.md`.

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 46 | Add `test_izhikevich_rs_preset_params` — RS preset a/b/c/d within physiological range (uncalibrated warning in test name) | `tests/test_emitters_networks.py` | pytest |
| 47 | Add `test_izhikevich_fs_preset_params` — FS preset | same | pytest |
| 48 | Add `test_izhikevich_chattering_preset_params` | same | pytest |
| 49 | Add `test_eig_network_excitatory_majority` — E-cell fraction ≥ 0.5 for default EIG | same | pytest |
| 50 | Add `test_eig_connectivity_sparsity` — connectivity density `< 1.0` for n ≥ 5 | same | pytest |
| 51 | Add `test_simulate_izhikevich_eig_shape` — voltage shape `(n_steps, n_cells)` | same | pytest |
| 52 | Add `test_simulate_izhikevich_eig_spike_bool` — spikes are boolean array | same | pytest |
| 53 | Add `test_eig_network_json_safe` — `EIGNetwork.to_dict()` serializes without NaN/Inf | same | pytest |
| 54 | Add `test_no_biological_cell_type_claim` — metadata contains `"cell_type_claim": "not_claimed"` or absent | same | pytest |
| 55 | Add `test_edge_list_receptor_range` — all `receptor_index` values in `{0, 1, 2, 3}` | same | pytest |
| 56 | Add `test_make_edge_list_from_dense_weight_preservation` — `sum(abs(weights))` matches dense matrix sum | same | pytest |
| 57 | Add `test_make_edge_list_sign_to_receptor` — positive weight → `receptor_index == 0` (AMPA), negative → `receptor_index == 1` (GABA_A) | same | pytest |
| 58 | Add `test_edge_backend_vmap_seed_batch_shape` — batch axis 0 is n_seeds | same | pytest |
| 59 | Add `test_edge_backend_jit_recompilation_count` — `jit` compiles exactly once for same shape | same | pytest |
| 60 | Commit emitter/network hardening | — | 203 + 15 = **218 passed** |

---

## Phase 4 — Objectives / Spectrolaminar (Actions 61–72)

> Targets `05_objectives_spectrolaminar_omission_plan_steps_301_400.md`.

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 61 | Add `test_objective_report_json_roundtrip` — `ObjectiveReport.to_dict()` → `json.dumps` → `json.loads` → reconstruct | `tests/test_objective_report_v018.py` | pytest |
| 62 | Add `test_objective_report_frozen` — mutation raises `FrozenInstanceError` | same | pytest |
| 63 | Add `test_evaluate_report_truth_gate_passthrough` — all 8 gates in report metadata | same | pytest |
| 64 | Add `test_spectral_objective_no_hz_claim` — `ObjectiveReport.metadata["frequency_unit"]` absent or `"relative_proxy"` | `tests/test_objectives.py` | pytest |
| 65 | Add `test_gamma_band_power_scaffold_only` — objective name contains `"scaffold"` or metadata marks uncalibrated | same | pytest |
| 66 | Add `test_omission_suppression_ratio_bounded` — suppression ratio in `[0.0, 1.0]` | same | pytest |
| 67 | Add `test_objective_null_baseline` — random network baseline is non-trivial (mean ≠ target) | same | pytest |
| 68 | Document `ObjectiveReport` canonical API note in README API table | `README.md` | review |
| 69 | Add `test_readout_spec_feature_names_unique` — `ReadoutSpec.features` has no duplicate names | `tests/test_readout_spec_v017.py` | pytest |
| 70 | Add `test_readout_result_shapes_match_spec` — each result array matches declared spec shape | same | pytest |
| 71 | Add `test_compute_readout_truth_gate_propagation` — all 8 gates in `ReadoutResult.metadata` | same | pytest |
| 72 | Commit objectives/readout hardening | — | 218 + 12 = **230 passed** |

---

## Phase 5 — Runtime / IO / Validation (Actions 73–82)

> Targets `07_runtime_io_manifest_validation_plan_steps_501_600.md`.

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 73 | Add `test_run_receipt_id_deterministic` — same `(cfg_hash, seed, version)` → same `receipt_id` | `tests/test_run_receipt_v016.py` | pytest |
| 74 | Add `test_run_receipt_id_version_sensitivity` — different version → different `receipt_id` | same | pytest |
| 75 | Add `test_run_receipt_frozen` — mutation raises `FrozenInstanceError` | same | pytest |
| 76 | Add `test_run_receipt_json_roundtrip` | same | pytest |
| 77 | Add `test_save_receipt_creates_file` — `save_receipt(r, path)` writes a valid JSON file | `tests/test_run_receipt_v016.py` | pytest |
| 78 | Add `test_io_json_safe_nan_rejection` — `json_safe()` replaces NaN with sentinel string | `tests/test_io.py` | pytest |
| 79 | Add `test_validation_unknown_key_warning_not_error` — extra key in config → `result.valid == True`, warning present | `tests/test_config_schema_v015.py` | pytest |
| 80 | Add `test_validation_all_required_sections_present` — missing each of `run/truth/network/emitter/field/probes` → `valid == False` | same | pytest |
| 81 | Add `test_config_hash_changes_on_mutation` — changing any field changes `config_hash` | same | pytest |
| 82 | Commit runtime/IO hardening | — | 230 + 10 = **240 passed** |

---

## Phase 6 — Examples / Docs / API Surface (Actions 83–92)

> Targets `08_examples_docs_colab_release_plan_steps_601_700.md`.

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 83 | Add `examples/07_run_receipt_readout_report.py` — canonical v0.1 three-method flow: `run_receipt → compute_readout → evaluate_report` | `examples/` | `PYTHONPATH=. python examples/07_...py` exits 0 |
| 84 | Add `examples/08_jcfg_json_workflow.py` — load `.jcfg.json`, validate, run, save receipt | `examples/` | same |
| 85 | Add `examples/09_multi_trial_batch.py` — `TrialBatch`, `config_to_trial_batch`, seed policy | `examples/` | same |
| 86 | Update README `## Examples` table with examples 07–09 | `README.md` | review |
| 87 | Add `docs/api_reference_v01.md` — one-page canonical v0.1 API cheat sheet (classes, factories, aliases) | `docs/` | review |
| 88 | Add `docs/truth_gate_contract.md` — enumerates all 8 gates, their allowed values, escalation policy | `docs/` | review |
| 89 | Add `docs/jcfg_schema_reference.md` — all `.jcfg.json` top-level keys, required vs optional, schema version | `docs/` | review |
| 90 | Add `CONTRIBUTING.md` — branch policy, truth gate contract, test requirements, AGENTS.md checklist | root | review |
| 91 | Add `docs/SECURITY.md` — no-secrets policy, no biological truth claim policy, data handling | `docs/` | review |
| 92 | Commit examples + docs block | — | push |

---

## Phase 7 — CI / Packaging (Actions 93–101)

| # | Action | File(s) | Gate |
|---|--------|---------|------|
| 93 | Create `.github/workflows/ci.yml` — `pytest -q`, Python 3.10/3.11/3.12 matrix, no GPU | `.github/workflows/` | workflow passes |
| 94 | Add `PYTHONPATH=.` to CI env so example sub-process tests pass | same | CI green |
| 95 | Add `python -m compileall -q jaxfne tests examples` step to CI | same | CI green |
| 96 | Add no-secrets grep step to CI (`grep -RInE 'api[_-]?key|secret|token|password'`) | same | CI green |
| 97 | Pin test dependencies in `pyproject.toml` `[project.optional-dependencies.dev]` with lower bounds | `pyproject.toml` | `pip install -e '.[dev]'` clean |
| 98 | Add `[tool.pytest.ini_options]` to `pyproject.toml` — `testpaths = ["tests"]`, `addopts = "-q"` | `pyproject.toml` | pytest invocation clean |
| 99 | Add `[tool.ruff]` lint config to `pyproject.toml` — select E/F/W, ignore E501 | `pyproject.toml` | `ruff check jaxfne/` clean |
| 100 | Run `ruff check jaxfne/ --fix` and fix any auto-fixable lint issues | `jaxfne/*.py` | `ruff check jaxfne/` clean |
| 101 | Final release gate: push `dev`, merge to `main`, create tag `v0.1.1`, push tag | — | `git tag v0.1.1` |

---

## Summary Table

| Phase | Actions | Outcome |
|-------|---------|---------|
| 0 — Release pipeline | 1–7 | v0.1.0 live on main + tag |
| 1 — Synaptic kernel v0.1.1 | 8–28 | 188 tests, receptor-exponential kernel |
| 2 — Source/field physics | 29–45 | 203 tests, conservation + gauge tests |
| 3 — Emitter/network hardening | 46–60 | 218 tests, preset + connectivity tests |
| 4 — Objectives/spectrolaminar | 61–72 | 230 tests, ObjectiveReport hardening |
| 5 — Runtime/IO/validation | 73–82 | 240 tests, receipt + IO hardening |
| 6 — Examples/docs | 83–92 | 3 new examples, 5 new doc files |
| 7 — CI/packaging | 93–101 | GitHub Actions CI, lint, v0.1.1 tag |

**Truth gate contract: frozen throughout all 101 actions.**
`truth_mode = truth_safe_unverified | claim_level = computational_scaffold | physical_amplitude_claim_allowed = False`
