## What this changes

<!-- One or two sentences: what does this PR do and why. -->

## Validation

- [ ] `python3 -m pytest tests/ -q -m "not slow"` passes
- [ ] `python3 -m compileall -q jaxfne` clean
- [ ] `python3 -m mkdocs build --strict` clean (if docs changed)
- [ ] No new public API without a test + doc entry

## Notes for reviewers

<!-- Anything reviewers should focus on, or context they need. -->
