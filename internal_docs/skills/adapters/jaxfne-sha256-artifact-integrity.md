# jaxfne-sha256-artifact-integrity

**Triggers:** sha256, checksum, artifact hash, stale output, reproducibility, manifest integrity.

**Purpose:** Content identity for figures, JSON, notebooks, and release artifacts. Same bytes → same hash.

**Note:** Hashes prove content identity, not biological truth. Pair with finite-output checks and execution receipts.

**Full skill:** user-installed `jaxfne-sha256-artifact-integrity`.
