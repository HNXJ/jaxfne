# jaxfne-code-orientation

**Triggers:** understand repo, where is X, pipeline, architecture, orient.

**Purpose:** Map Emitter→Source→Field→Probe→Objective→Optimizer from source, not memory.

**Start here:**

```bash
git rev-parse --show-toplevel
python -c "import jaxfne as jtfne; print(jtfne.__version__)"
```

Read `internal_docs/loop_context/02_PUBLIC_API_CONTRACT.md` and package `jaxfne/__init__.py` exports.

**Full skill:** user-installed `jaxfne-code-orientation`.
