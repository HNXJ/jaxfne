{
  "id": "pkg-pipeline",
  "schema_version": 3,
  "kind": "module",
  "title": "_pipeline.py",
  "generated": {
    "summary": "\"\"\"Internal pure-function layer for the Configuration/Tensor/Model/Signals -- pipeline (Phase 0 of the planned 4-object public API consolidation).",
    "source_paths": [
      "jaxfne/_pipeline.py"
    ],
    "links": [
      {
        "to": "jaxfne-pkg",
        "relation": "refines"
      }
    ]
  },
  "status": "verified",
  "notes": [
    {
      "id": "module-formation-pass-2026-07-23",
      "kind": "module_investigation",
      "date": "2026-07-23",
      "source": "multi-agent Labyrinth module-level formation pass (Workflow tool, one agent per module/group/subpackage), spot-checked before commit",
      "confidence": "High for line numbers, imports, git state, and FRICTIONS_STACK.md citations (all directly grepped/read this session). Moderate on downstream semantic correctness of the HDP kernel internals (e.g. emitters.py line ~1298/~1368 references) since emitters.py itself was not independently re-read this pass -- those citations are carried from this file's own docstrings, not re-verified.",
      "purpose": "jaxfne/_pipeline.py (389 lines) is an internal (not-yet-public) pure-function wrapper layer for the planned Configuration/Tensor/Model/Signals object-consolidation (\"Phase 0\"/\"Phase 2\" per its module docstring, lines 1-34). It wraps already-verified logic (construct/simulate/neuronal_tensor_to_configuration, load/save_neuronal_tensor) with thin passthrough functions, and additionally implements one genuinely new capability: a JIT-compiled single-HDP-step function (compile_step_fn/scan_network) plus checkpoint/restore of model.params to .npz+JSON. It is imported into the public namespace at jaxfne/__init__.py:230 (DynamicState, dynamic_state_from_model, checkpoint_state, restore_state, compile_step_fn, scan_network) even though the module docstring says it is \"Not part of the public API yet.\"",
      "key_findings": [
        "389 lines total. No TODO/FIXME/NotImplementedError anywhere (grep empty).",
        "Working tree clean for this file: `git status --short -- jaxfne/_pipeline.py` returned nothing -- last commit touching it is c3b4889 'fix: add record_weight_trace opt-out to HDP kernel and Phase-2 pipeline, closing the edge-trace memory-scaling OOM'.",
        "Key functions: load_tensor/save_tensor (79-88), tensor_to_configuration (91-103), build_network (106-119), run_network (122-127), select_signal (130-135), initialize_dynamic_state/initialize_static_state (138-164), checkpoint_state/restore_state (167-228), dynamic_state_from_model (231-264), compile_step_fn (267-369), scan_network (372-389).",
        "class DynamicState(NamedTuple) at line 57: fields v, u, prev_spikes, syn_state, H, w -- the canonical HDP edge-list carry tuple, order-matched to emitters.simulate_edge_recurrent_izhikevich_hdp's internal unpack (docstring lines 58-69).",
        "Deliberately NOT implemented (docstring lines 12-15): configuration_to_tensor / tensor_to_graph / a general select_signal-equivalent requiring unverified Model/EIGNetwork internals -- documented scope boundary, not a gap.",
        "Docstring lines 29-33 record a RESOLVED item: legacy JaxFNEConfig format and 21 dependent tests deleted 2026-06-30 -- config_to_configuration/config_to_simulation/config_to_geometry/load_config/validate_config/config_truth_boundary/config_to_trial_batch no longer exist."
      ],
      "contracts": [
        "compile_step_fn requires model.params['edge_list'] present (dynamic_state_from_model raises ValueError otherwise, lines 243-248) -- canonical Phase-2 execution mode is HDP + edge-list backend only; dense-only/non-HDP models unsupported by design.",
        "checkpoint_state/restore_state round-trip: bfloat16 leaves upcast to float32 before np.savez, original dtype name recorded in JSON sidecar (schema 'checkpoint_v1', line 198), restore_state casts back down using that name (lines 176-183, 221-226) -- required because plain np.savez/np.load silently mangles ml_dtypes bfloat16 into raw |V2 bytes on read-back. restore_state raises ValueError on schema mismatch (line 220).",
        "restore_state returns (leaves, static) -- raw flattened leaves + JSON-safe static dict, not a reassembled Model; caller reassembles via jax.tree_util.tree_unflatten using a treedef from a freshly-built Model, never the disk-serialized treedef string (lines 204-215, explicitly non-reversible).",
        "compile_step_fn's step_fn xs_t is (sched_t, key_t): key_t is a per-step PRNGKey, not pre-sampled noise (lines 304-311); scan_network's `keys` arg must be a (n_steps, 2) array of split PRNGKeys.",
        "record_weight_trace (default True, line 273) controls only scan_network's stacked weight-trace output arity (4 vs 5 elements, lines 354-357), not carry.w itself -- disabling it never disables HDP plasticity."
      ],
      "landmines": [
        "F-023 skills/FRICTIONS_STACK.md:55 documents a real bug previously hit in this exact file: checkpoint_state/restore_state did NOT originally round-trip bfloat16 correctly (np.savez/np.load silently corrupts ml_dtypes bfloat16 into dtype('|V2') void bytes, no exception) -- fixed by the float32-upcast/dtype-sidecar scheme now present (lines 176-183); guarded by tests/test_pipeline_pure_functions.py::test_checkpoint_restore_roundtrip_preserves_bfloat16.",
        "Module docstring claims 'Not part of the public API yet -- not exported from jaxfne/__init__.py' (lines 3-4), but jaxfne/__init__.py:230 actually imports 6 names from it (DynamicState, dynamic_state_from_model, checkpoint_state, restore_state, compile_step_fn, scan_network) -- the docstring is stale/inaccurate versus the real public surface.",
        "compile_step_fn hardcodes n_steps=1 per call as a deliberate workaround (Option B, lines 294-311) because emitters.py's real step closure is a non-exported local closure -- documented intentional deviation, not a bug, but means every call re-enters the full simulate_edge_recurrent_izhikevich_hdp kernel rather than a lighter primitive."
      ],
      "related_nodes": [
        "tests/test_pipeline_pure_functions.py -- direct unit coverage (from jaxfne import _pipeline), including the bfloat16 checkpoint round-trip regression test",
        "tests/test_util_config_tensor.py -- also imports from jaxfne import _pipeline",
        "tests/test_sphere20_ei_hdp_habituation.py:165 -- imports compile_step_fn/scan_network for an HDP habituation scenario",
        "jaxfne/core.py -- Configuration/Model/Signals and construct()/simulate() wrapped by this module",
        "jaxfne/neuronal_tensor.py -- NeuronalTensor/RuntimeConfiguration/load/save_neuronal_tensor/neuronal_tensor_to_configuration wrapped by load_tensor/save_tensor/tensor_to_configuration",
        "jaxfne/emitters.py -- simulate_edge_recurrent_izhikevich_hdp, the kernel compile_step_fn wraps",
        "jaxfne/io.py -- json_safe, used by checkpoint_state for the static-metadata sidecar",
        "jaxfne/__init__.py:230 -- the actual export line contradicting this module's 'not public yet' docstring claim"
      ]
    }
  ],
  "issues": [],
  "plan": "",
  "verification": {
    "checks": {
      "sources_resolve": true,
      "reproducible": true
    },
    "verdict": "verified",
    "checks_run": 2
  }
}
